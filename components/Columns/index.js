import React from "react";
import Copy from '../blocks/Copy'

const Columns = () =>{

  return(
    <section className="section-default section-color__alternative">
      <Copy title="Working where our clients are." copy="Our team has an office in New York, working with clients as an extension of their team—onsite, near-site, or wherever it makes sense." />
    </section>
  )
}
export default Columns